version: "3.7"

services:
  notifier:
    hostname: notifier
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    environment:
      DEBUG: "true"
      APP_PORT: "8888"
    volumes:
      - .:/app
    ports:
      - "8888:8888"